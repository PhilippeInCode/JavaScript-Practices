/* Ejercicio 9: Bucle For...of
Crea un array de nombres y utiliza un bucle for...of para imprimir cada nombre en la consola. */

let nombres = ["Judith", "Pablo", "Javi", "Mario", "Felipe"];

    for(let i = 0; i < nombres.length; i++){
        console.log("Nombre en la posición " + i + " corresponde al nombre: " + nombres[i]);
    }
